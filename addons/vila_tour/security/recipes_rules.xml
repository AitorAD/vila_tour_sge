<odoo>
    <data noupdate="1">
        <record id="recipe_user_rule" model="ir.rule">
            <field name="name">User Own Recipes</field>
            <field name="model_id" ref="model_recipes"/>
            <field name="domain_force">[('creator_id', '=', user.id)]</field>
            <!-- 
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            -->
            <field name="groups" eval="[(4, ref('group_vila_tour_user'))]"/>
            <field name="perm_read" eval="1"/>
            <field name="perm_write" eval="1"/>
            <field name="perm_create" eval="1"/>
            <field name="perm_unlink" eval="1"/>
        </record>
    </data>
</odoo>